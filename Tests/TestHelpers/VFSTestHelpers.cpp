#include "VFSTestHelpers.h"
#include <system_error>

namespace entropy::test_helpers {
// Definitions are in the header for these simple RAII helpers. This translation unit
// ensures the helpers are linked into each test executable (as CMake adds this file).
}
